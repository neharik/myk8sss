apiVersion: apps/v1
kind: Deployment
metadata:
  name: myjavaapp-deploy
  labels: 
    app: myjavaapp
spec:
  repicas: 3
  selector:
    matchLabels:
      app: myjavaapp
  template:
    metadata:
      labels:
        app: myjavaapp
    spec:
      containers:
      - name: myjavaapp-container
        image: neharika/myjavaimage24may22:1
        ports:
        - containerPort: 8080
